// Styling shared by the US and world maps, so both draw borders the same way.

/**
 * Border color between states and countries. Medium gray, so borders stand out against the
 * light-gray "not visited" fill (they used to be white and nearly vanished) and still read
 * against visited fills. The land fills are the same in light and dark mode, so one value
 * works for both.
 */
export const MAP_BORDER = "#8b929c";
